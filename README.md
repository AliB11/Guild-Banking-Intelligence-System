# GBI — سامانه هوش بانکداری اصناف

داشبورد تحلیل سودآوری، ماتریس رسته‌های شغلی، قیف سرنخ شعب و ماشین‌حساب تسهیلات برای بانکداری اصناف.

## اجرای سریع

```bash
npm install
cp .env.example .env
# مقدار DATABASE_URL را در .env تنظیم کنید
npm run db:push
npm run db:seed
npm run dev
```

- برنامه روی `0.0.0.0` اجرا می‌شود تا در Preview/Container قابل دسترسی باشد.
- اگر `DATABASE_URL` تنظیم نشده باشد، برنامه عمداً در **حالت نمایشی** با یک corpus درون‌حافظه‌ای کامل اجرا می‌شود؛ در این حالت هیچ اطلاعاتی در PostgreSQL نوشته نمی‌شود و نوار وضعیت برنامه آن را نمایش می‌دهد.
- `npm run db:seed` داده‌های نمونه را در یک transaction جایگزین می‌کند. این دستور را روی دیتابیس عملیاتی اجرا نکنید؛ در `NODE_ENV=production` فقط با `ALLOW_DESTRUCTIVE_SEED=true` اجرا می‌شود.

## اسکریپت‌ها

| دستور | کاربرد |
| --- | --- |
| `npm run dev` | توسعه با میزبان قابل Preview |
| `npm run build` | build تولیدی |
| `npm run start` | اجرای build تولیدی |
| `npm run lint` | بررسی ESLint |
| `npm run typecheck` | بررسی TypeScript |
| `npm run db:push` | اعمال schema با Drizzle؛ نیازمند `DATABASE_URL` |
| `npm run db:seed` | درج corpus نمونه در PostgreSQL |
| `npm run db:studio` | باز کردن Drizzle Studio |

## قرارداد محاسباتی مهم

تمام مبالغ در لایه داده و موتور محاسباتی **ریال** هستند و فقط در لایه نمایش به تومان تبدیل می‌شوند. امتیاز سرنخ از چهار جزء رسوب، گردش، تناسب اعتباری و انطباق مالیاتی ساخته می‌شود. `GET /api/health` در حالت واقعی اتصال PostgreSQL و در حالت بدون پیکربندی، وضعیت demo را گزارش می‌کند.

## موارد اصلاح‌شده در بازبینی ۳۶۰ درجه

- حذف خطای build ناشی از پرتاب `DATABASE_URL is required` هنگام import شدن ماژول دیتابیس؛ اتصال اکنون lazy است و حالت نمایشی کنترل‌شده دارد.
- اضافه شدن fallback کامل و قابل مشاهده برای اجرای UI در checkout تازه، به‌علاوه error/loading boundary.
- جلوگیری از race condition ماشین‌حساب؛ پاسخ قدیمی شبکه دیگر نتیجه سناریوی جدید را overwrite نمی‌کند.
- اصلاح ROI از «درآمد ناخالص / هزینه» به «حاشیه خالص / هزینه».
- اصلاح مقدار پیش‌فرض سبد خرید که با واحد اسلایدر (تومان) هم‌خوان نبود.
- جلوگیری از setState در render در پنل مقایسه و حذف setState همگام داخل effect کانبان.
- optimistic update امن برای جابه‌جایی سرنخ با rollback هنگام خطا و آمار لحظه‌ای ستون‌ها.
- اصلاح محاسبه میانگین امتیاز رسته‌ها در حضور پذیرنده بدون metric دوره جاری.
- اصلاح سهم بازار رادار از «نرمال‌شده نسبت به بزرگ‌ترین رسته» به سهم واقعی از کل گردش.
- جلوگیری از درصد رشد جعلی در اولین دوره و محدود کردن روند به شش دوره اخیر.
- نمایش دو محور واقعی برای نمودار گردش/رسوب، empty state برای نمودارهای بدون داده و پشتیبانی از reduced motion.
- افزودن unique index برای `(merchant_id, reporting_period)` و تبدیل Drizzle config به تنظیمات مبتنی بر env.
- اصلاح timestampهای seed که قبلاً سال جلالی را مستقیماً به `Date` میلادی می‌دادند.
- ارتقای Next/PostCSS به نسخه‌های بدون آسیب‌پذیری runtime گزارش‌شده و اضافه شدن بررسی health.

## پیشنهادهای خلاقانه برای مرحله بعد

1. **Next Best Action برای هر سرنخ:** به‌جای فقط امتیاز، اقدام بعدی، کانال تماس، شعبه مسئول و مهلت پیگیری را پیشنهاد دهید و اثر اقدام را با A/B test بسنجید.
2. **آزمایشگاه سناریوی نقدینگی:** شوک افت ۲۰٪ گردش، افزایش CCC، تغییر نرخ ذخیره قانونی و تأخیر تسویه PSP را هم‌زمان روی سود بانک و سقف اعتبار شبیه‌سازی کنید.
3. **نقشه فرصت شعب:** ترکیب شدت گردش، ظرفیت خالی پایانه، فاصله شعبه تا پذیرنده و احتمال تبدیل روی یک نقشه اولویت‌دار؛ خروجی آن برنامه هفتگی تیم فروش باشد.
4. **امتیاز سلامت و هشدار زودهنگام:** افت متوالی گردش، جهش chargeback، کاهش رسوب یا تغییر MCC را به‌صورت explainable به هشدار تبدیل کنید؛ نه یک black-box score.
5. **شبکه تأمین قابل کشف:** از ارتباط پذیرندگان، تأمین‌کنندگان و مشتریان عمده یک گراف بسازید تا پیشنهاد SCF/GAM از روی روابط واقعی زنجیره صادر شود.
6. **کنترل کیفیت داده پیش از KPI:** رکوردهای duplicate، دوره‌های جاافتاده، تغییر ناگهانی MCC، مبلغ خارج از بازه و ناسازگاری تعداد پایانه با گردش را قبل از ورود به dashboard quarantine کنید.
7. **حاکمیت مدل و audit trail:** نسخه موتور، ورودی، دلیل پیشنهاد محصول، کاربر تأییدکننده و timestamp را کنار هر lead/quote ذخیره کنید تا تصمیم اعتباری قابل بازبینی باشد.
8. **امنیت عملیاتی قبل از production:** احراز هویت سازمانی، RBAC شعبه/ستاد، audit log غیرقابل‌تغییر، rate limit، CSRF protection و masking اطلاعات هویتی را پیش از اتصال داده واقعی الزامی کنید.
9. **معماری رویدادمحور:** به‌جای refresh کامل جداول، ingestion تراکنش و metric را با outbox/CDC به صف ببرید؛ cache داشبورد را با invalidation دقیق به‌روز کنید.
10. **بودجه‌ریزی قابل توضیح:** کنار هر نمودار سهم «رسوب / کارمزد / اعتبار / هزینه پشتیبانی» و confidence interval نمایش دهید تا مدیر بتواند فرق داده مشاهده‌شده و برآورد مدل را ببیند.

این سامانه تصمیم‌یار است و سقف اعتبار یا نرخ واقعی باید با سیاست اعتباری، بخشنامه‌های جاری، احراز هویت و کنترل‌های ریسک بانک تطبیق داده شود.
