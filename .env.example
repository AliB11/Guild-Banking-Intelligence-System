# PostgreSQL connection string used by Next.js and Drizzle.
# The app renders a clearly marked in-memory demo when this is absent,
# but production deployments should always configure a real database.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
